---
name: doc-sync
description: |
  実装変更に伴うドキュメント自動同期。スキーマ・API・ファイル構造・コマンドの変更を検知し、対応ドキュメントを更新する。
  TRIGGER when: drizzle/schema.ts の変更、新規ファイル・ディレクトリ追加、tRPC ルーター変更、package.json scripts 変更、spec/ 配下の仕様書変更。「ドキュメント同期」「docs更新」。
  DO NOT TRIGGER when: ドキュメントのみの変更、テストのみの変更、doc-garden（包括的チェック）実行時。
---

# doc-sync — 実装変更に伴うドキュメント自動同期

## 背景
/sign 開発でドキュメントと実装の乖離が多発。「後で直す」は禁止。変更時に即座に修正する。

## トリガーと必須アクション

### 1. スキーマ変更時
`drizzle/schema.ts` にテーブルやカラムを追加・変更・削除した場合:
- `docs/domain/data-model.md` のエンティティ一覧とテーブル詳細を更新
- `CLAUDE.md` の「ドメインモデル」行を更新
- `docs/architecture.md` のテーブル数記載を更新

### 2. 新規ファイル・ディレクトリ追加時
- `docs/architecture.md` の該当レイヤーにパスが記載されているか確認
- 記載がなければ追加（主要ファイルのみ。一時ファイルやテストは不要）

### 3. API エンドポイント変更時
tRPC ルーターや Express エンドポイントを追加・変更した場合:
- `docs/architecture.md` の RBAC 表を確認・更新

### 4. package.json scripts 変更時
scripts を追加・変更・削除した場合:
- `CLAUDE.md` の「コマンド」セクションを更新

### 5. 機能仕様書（spec/）変更時
`spec/` 配下の仕様書を追加・変更・削除した場合:
- `Spec.md`（プロジェクト概要仕様）の該当セクションを確認・更新

### 6. エージェントが躓いた時
ドキュメントの記載が古い・不正確で作業に支障が出た場合:
- 該当ドキュメントをその場で修正
- `lessons.md` に教訓を追記

## 原則
- ドキュメント修正は実装変更と **同一コミット** に含める
- 構造テスト（`server/structural.test.ts`）がドキュメント乖離を検出した場合、テストではなくドキュメントを修正する
